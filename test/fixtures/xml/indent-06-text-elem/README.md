# indent-06-text-elem

Design §6.3.2 byte-exact example: mixed content `[text, element]`.
Leading text emits inline (no prior body chunk), element gets its
own indented line, close tag indented because body ends with `>`.
