# 23-attr-order

§8 row 23: attribute keys sorted alphabetically by the writer's
splitAttrsAndChildren. Input provides keys in out-of-order shape;
expected output lists them alpha-sorted (`alpha`, `mike`, `zulu`).
