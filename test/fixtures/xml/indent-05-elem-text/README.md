# indent-05-elem-text

Design §6.3.2 byte-exact example: mixed content `[element, text]`.
Trailing text after an element pins rule 3 — text after a
`>`-terminated sibling gets its own indented line. Close tag inline
because body ends with text (not `>`).
